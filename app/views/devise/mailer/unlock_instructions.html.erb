<p><%= "¡Hola #{@resource.email}!" %></p>

<p><%= "Su cuenta se ha bloqueado porque se han superado el número máximo de intentos para conectarse." %></p>

<p><%= t('.instruction', :default => "Para desbloquear su cuenta ingrese al siguiente link:" %></p>

<p><%= "Desbloquear mi cuenta", unlock_url(@resource,
                                           unlock_token: @resource.unlock_token,
                                           locale: I18n.locale) %></p>
